# Base
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
python-dotenv==1.0.1

# Numpy stack — numpy 1.26 pour compat rapidocr
numpy==1.26.4
Pillow==10.4.0
opencv-python-headless==4.10.0.84
scipy==1.11.4

# OCR engines rapides / prod
rapidocr-onnxruntime==1.3.24         # "paddle" (modèles PP-OCR en ONNX)
surya-ocr>=0.6                       # OCR multi-langue open source
google-generativeai==0.8.3           # Gemini API
protobuf>=4.25,<6

# OCR engines lourds (transformers-based) — nécessitent torch
# torch est déjà installé via surya
transformers>=4.45
accelerate>=0.30
sentencepiece>=0.2                   # pour GOT-OCR2
tiktoken>=0.7                        # pour GOT-OCR2
qwen-vl-utils>=0.0.8                 # pour Qwen2.5-VL

# NB: torch est pull par surya-ocr
# GPU CUDA recommandé pour Qwen/MiniCPM/GOT — sur CPU c'est très lent
